[application]
name = "event-badge"
description = "Proof of Attendance Badge application on Linera microchains"
version = "0.1.0"

[contract]
name = "event-badge-contract"
wasm_path = "target/wasm32-unknown-unknown/release/event_badge_contract.wasm"

[service]
name = "event-badge-service"
wasm_path = "target/wasm32-unknown-unknown/release/event_badge_service.wasm"

[[graphql_api]]
name = "event-badge"
endpoint = "/graphql"
